apiVersion: v1
kind: Service
metadata:
  name: cicd-demo-app
  namespace: demo
spec:
  type: ClusterIP
  selector:
    app: cicd-demo-app
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
